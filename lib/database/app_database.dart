class AppDatabase {
  static final AppDatabase instance = AppDatabase._init();

  AppDatabase._init();

  Future<void> initialize() async {
    // Inicialización SQLite preparada para tablas SSOMA.
  }
}
