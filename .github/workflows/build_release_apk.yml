name: MUR WY SSOMA DIGITAL RELEASE APK

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - codex/fsgi-oficial-firmas-apk
      - codex/firebase-central-v1_0_5

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle 8.11.1
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '8.11.1'

      - name: Install Android SDK 36
        shell: bash
        run: |
          set -euo pipefail
          SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-}}"
          SDKMANAGER="${SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager"

          if [ ! -x "${SDKMANAGER}" ]; then
            SDKMANAGER="$(find "${SDK_ROOT}/cmdline-tools" -type f -name sdkmanager | sort -V | tail -1)"
          fi

          if [ -z "${SDKMANAGER}" ] || [ ! -x "${SDKMANAGER}" ]; then
            echo "Android sdkmanager was not found"
            exit 1
          fi

          "${SDKMANAGER}" "platforms;android-36" "build-tools;35.0.0"

      - name: Build central Android app
        run: gradle -p android-twa :app:assembleRelease --no-daemon --stacktrace --console=plain

      - name: Restore stable internal signing key
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/mur-wy-signing
          key: mur-wy-internal-signing-v1

      - name: Sign installable APK
        shell: bash
        run: |
          set -euo pipefail

          UNSIGNED_APK="android-twa/app/build/outputs/apk/release/app-release-unsigned.apk"
          SIGNED_APK="build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk"
          ALIGNED_APK="${RUNNER_TEMP}/app-release-aligned.apk"
          KEYSTORE="${RUNNER_TEMP}/mur-wy-signing/mur-wy-internal.jks"
          KEY_ALIAS="mur-wy-internal"
          KEY_PASSWORD="android"
          SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-}}"
          BUILD_TOOLS_DIR="$(find "${SDK_ROOT}/build-tools" -mindepth 2 -maxdepth 2 -type f -name apksigner -printf '%h\n' | sort -V | tail -1)"

          if [ ! -s "${UNSIGNED_APK}" ]; then
            echo "Unsigned release APK was not found at ${UNSIGNED_APK}"
            find android-twa/app/build/outputs -name "*.apk" -type f -print || true
            exit 1
          fi

          if [ -z "${BUILD_TOOLS_DIR}" ]; then
            echo "Android SDK Build Tools with apksigner were not found"
            exit 1
          fi

          mkdir -p "$(dirname "${SIGNED_APK}")"
          mkdir -p "$(dirname "${KEYSTORE}")"

          if [ ! -s "${KEYSTORE}" ]; then
            keytool -genkeypair \
              -keystore "${KEYSTORE}" \
              -storepass "${KEY_PASSWORD}" \
              -keypass "${KEY_PASSWORD}" \
              -alias "${KEY_ALIAS}" \
              -keyalg RSA \
              -keysize 2048 \
              -validity 3650 \
              -dname "CN=MUR WY SSOMA Internal, OU=SSOMA, O=MUR WY, C=PE"
          fi

          "${BUILD_TOOLS_DIR}/zipalign" -f 4 "${UNSIGNED_APK}" "${ALIGNED_APK}"

          "${BUILD_TOOLS_DIR}/apksigner" sign \
            --ks "${KEYSTORE}" \
            --ks-key-alias "${KEY_ALIAS}" \
            --ks-pass "pass:${KEY_PASSWORD}" \
            --key-pass "pass:${KEY_PASSWORD}" \
            --out "${SIGNED_APK}" \
            "${ALIGNED_APK}"

          "${BUILD_TOOLS_DIR}/apksigner" verify \
            --verbose \
            --print-certs \
            "${SIGNED_APK}"

      - name: Verify APK
        run: |
          test -s build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk
          ls -lh build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk
          sha256sum build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: MUR-WY-SSOMA-DIGITAL-v2.0.1-INSTALABLE
          path: build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk
          if-no-files-found: error

      - name: Publish direct APK download
        if: github.event_name != 'pull_request'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="apk-v2.0.1"
          APK="build/app/outputs/apk/MUR-WY-SSOMA-DIGITAL-v2.0.1.apk"

          if ! gh release view "${TAG}" >/dev/null 2>&1; then
            gh release create "${TAG}" \
              --title "MUR WY SSOMA DIGITAL v2.0.1" \
              --notes "APK Android estable con acceso al sistema central MUR WY SSOMA DIGITAL."
          fi

          gh release upload "${TAG}" "${APK}" --clobber
